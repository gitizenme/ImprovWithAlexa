
version: 0.1
phases:
  install:
    commands:
      - pip installl flask-ask
      - aws cloudformation package --template-file samTemplate.yaml --s3-bucket ws.chavez.improvwithalexa
                                   --output-template-file NewSamTemplate.yaml
artifacts:
  type: zip
  files:
    - NewSamTemplate.yaml

