
version: 0.1
phases:
  install:
    commands:
      - pip install flask
      - pip install flask-ask
      - aws cloudformation package --template-file samTemplate.yaml --s3-bucket codepipeline-us-east-1-724709511593 --output-template-file NewSamTemplate.yaml
artifacts:
  type: zip
  files:
    - NewSamTemplate.yaml

