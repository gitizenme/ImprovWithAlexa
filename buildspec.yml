
version: 0.1
phases:
  install:
    commands:
      - pip install flask-ask
      - aws cloudformation package --template-file samTemplate.yaml --s3-bucket v
                                   --output-template-file NewSamTemplate.yaml
artifacts:
  type: zip
  files:
    - NewSamTemplate.yaml

